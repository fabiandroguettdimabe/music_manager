<resources>
    <!-- Tema base de la Activity (Compose pinta el resto). Negro absoluto, sin ActionBar. -->
    <style name="Theme.Noir" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/noir_black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash screen (Android 12+ y retro-compatible vía core-splashscreen). -->
    <style name="Theme.Noir.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/noir_black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.Noir</item>
    </style>
</resources>
